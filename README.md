# codix-suite

This project is devoted to the codix-suite software

## System dependencies

- Python ≥ 3.9
- `tkinter` (included in the standard Python distribution, but may require :
  - under Ubuntu/Debian: `sudo apt install python3-tk`
  - on macOS : install Python from python.org
)
